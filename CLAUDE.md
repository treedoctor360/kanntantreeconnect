# CLAUDE.md — かんたん樹木登録アプリ（kanntantreeconnect）

最終更新: 2026-08-23
担当: Koh Kitsukawa（大津市公園緑地課 / 樹木医）

---

## 0. 目的とスコープ

**作業中に「この木、番号を振っておきたい」と思った瞬間に、3タップで1本を登録できる道具。**

位置・樹種・番号・写真に加えて、**紙の「樹木点検 現地チェックシート」1ページ目の点検内容**を残す（14章）。
健全度・活力度の判定や診断所見は行わない。現場で〇を付けるのと同じ粒度までにとどめる。

### 既存システムとの関係（重要）

| システム | 役割 | 本アプリとの関係 |
|---|---|---|
| 樹木診断システム v27（`shindanv27`） | 個体単位の精密な診断カルテ | **座標入力UIを流用する**（後述3章）。将来の連携先 |
| 樹木台帳メモ（`parktreenote`） | 現場メモ・数量集計（樹種＋規格＋本数） | **公園登録の考え方を流用する**（後述4章） |
| サブ樹木医システム / 写真帳システム | — | 本アプリは干渉しない |

**やらないこと（明示的にスコープ外）**
- 健全度・活力度の判定
- 診断所見、対策提案
- オフライン動作（Service Worker / PWAキャッシュ）

**やること（本リポジトリで追加した部分）**
- **スプレッドシート連携（GAS）** — 13章。kantantree では「将来検討」としていたクラウド同期を、
  GASの中継Web App経由で実装した。アプリ起動ごとの取り込みと、書き換えごとの自動保存を行う。
  Supabase は使わない（Googleアカウントだけで完結させるため）。
- **点検内容（現地チェックシート1ページ目）** — 14章。紙のチェックシートの各行の項目
  （テープ番号・樹高・葉の茂り・キノコ・キノコ部位・空洞/傷・空洞/傷の位置・フラス・
  周辺環境・注意・写真）と、表頭（場所・調査日・調査者・テープロール）を
  そのまま入力できるようにした。

---

## 1. 前提条件（確定事項）

| 項目 | 決定 |
|---|---|
| 利用者 | 当面 Koh のみ。他職員が使う可能性あり（初版では考慮不要、データ形式だけ将来に備える） |
| 保存先 | 端末内（IndexedDB）＋ JSONバックアップの書き出し／読み込み ＋ **スプレッドシート（GAS経由・13章）** |
| オフライン | 不要 |
| 樹木番号 | 新規に自動採番（既存台帳の体系には合わせない） |
| 台帳連携 | 初版は「案A（ファイル受け渡し）」。ただしデータ構造は「案C（共通スキーマ）」を満たす形で作る（10章） |
| 写真 | 必要（1本あたり最大3枚） |
| 転職後の利用 | 未定。**大津市固有の値をコードに直書きしない**（公園名・樹種はすべてユーザーが登録するデータとして扱う） |

---

## 2. 技術スタック

v27 と同一構成にする（部品を流用するため）。ただし不要なものは削る。

| 層 | 採用 | 備考 |
|---|---|---|
| ビルド | Vite | v27 と同じ |
| UI | React | v27 と同じ |
| ローカルDB | Dexie.js | IndexedDB（ブラウザ内DB）の薄いラッパー |
| 状態管理 | **なし**（Dexie の `useLiveQuery` ＋ React の `useState`） | この規模では Zustand は過剰。v27 から削る |
| 地図 | React-Leaflet ＋ 国土地理院タイル | v27 と同じ |
| PWA | **なし**（`manifest.json` のみ置く） | オフライン不要のため Service Worker は入れない。ホーム画面追加のアイコンだけ欲しいので manifest は置く |
| 同期 | **GAS Web App（中継）＋ スプレッドシート** | 追加ライブラリなし。`fetch` で JSON を投げるだけ（13章） |
| デプロイ | GitHub Pages | `vite.config.js` の `base: '/kanntantreeconnect/'` |

> ライブラリのバージョンは着手時に最新の推奨を確認して確定すること。

---

## 3. 座標入力UI（v27 から流用・仕様固定）

**v27 の「カルテ」タブ内の座標ブロックと同一の見た目・挙動にする。**
参照元: `shindanv27` の `src/components/.../LocationPicker.jsx` 周辺。
実装前に必ず v27 の実ソースを読み、UI文言・関数名を合わせること。

### 3-1. 画面要素（上から順）

```
座標（近接木の距離判定に使用）     ← 見出し（本アプリでは「座標」だけでよい）
  緯度 Latitude   [ 35.01776443639602 ]   ← テキスト入力。フル桁を保持する
  経度 Longitude  [ 135.85462927807734 ]
  [📍 現在地を入れる] [クリア]  地図で確認   ← 3ボタン横並び（3つ目はリンク体）
  35.017764, 135.854629（現在地）           ← 現在地の参考表示（丸め6桁）
  [Googleマップのリンク / 35.0…] [読み取り]  ← 貼り付け欄＋読み取りボタン
  現在地を取得しました（誤差 約9m）。         ← ステータス行
  後日の登録でも、現地のGoogleマップのリンクや
  緯度経度を貼り付ければ座標を残せます。      ← 補足文（そのまま流用）
```

### 3-2. 挙動

- **📍現在地を入れる**
  `navigator.geolocation.getCurrentPosition` を
  `{ enableHighAccuracy: true, timeout: 10000, maximumAge: 0 }` で呼ぶ。
  成功したら緯度・経度欄を埋め、`accuracy`（m）を保持して
  「現在地を取得しました（誤差 約◯m）。」と表示する。
  失敗時はエラー内容に応じた日本語メッセージを出す（許可拒否／タイムアウト／取得不可）。
- **クリア** — 緯度・経度・accuracy・coordSource を空にする。
- **地図で確認** — 地図モーダルを開き、ピンをドラッグまたはタップで補正できる。
  補正したら `coordSource` を `'map'` にする。
  樹冠下では衛星電波の反射（マルチパス）でGPS精度が落ちるため、**手で直せる導線は必須**。
- **読み取り（Googleマップリンク）** — 貼り付けられた文字列から緯度経度を抽出する。
  対応させる形式は最低限これら:
  - `https://maps.google.com/?q=35.0177,135.8546`
  - `.../@35.0177,135.8546,18z`
  - `...!3d35.0177!4d135.8546`
  - `35.0177, 135.8546`（座標のみのベタ貼り）
  抽出できたら `coordSource` を `'link'` にする。できなければ「座標を読み取れませんでした」と表示。
- **手入力** — 緯度経度欄を直接編集した場合は `coordSource` を `'manual'` にする。

### 3-3. 保持する値

```js
{ lat: Number|null, lng: Number|null, accuracy: Number|null,
  coordSource: 'gps' | 'map' | 'manual' | 'link' | null }
```

座標は**必須にしない**。後日まとめて入れられることを補足文で明示しているため、
座標なしでも登録できる（一覧で「座標なし」バッジを出す）。

---

## 4. 公園登録機能（parktreenote から流用）

参照元: `parktreenote` の `index.html`。実装前に実ソースの公園まわりを読むこと。

### 4-1. 機能

- 公園の**追加・編集・削除**（設定タブ）
- **公園コード**（例 `P001`）— 樹木番号の接頭辞に使う。**登録時に自動付与、編集可**（詳細は4-2）
- **公園名**（例 `皇子が丘公園`）
- **代表座標** — 「📍現在地を入れる」で登録。地図タブの初期表示位置に使う
- **公園タブによる絞り込み** — 一覧タブの上部に公園タブを横スクロールで並べる
- **この公園をまるごと削除** — 配下の樹木・写真も削除。件数付きの確認ダイアログを出し、
  削除は単一の IndexedDB トランザクションで実行する（parktreenote v3.1 と同じ方式）
- **並び順** — 最終使用日時の降順（よく使う公園が左に来る）

### 4-2. 公園コードと樹木番号の採番

#### 公園コード

- 公園を登録した瞬間に **`P001`, `P002`, …** を自動付与する。
  登録時にコードを考えさせない（入力欄は初期値入りで表示する）。
- 採番は「既存の公園コードのうち `P` ＋数字 形式のものの最大値 ＋1」を毎回計算する。
  カウンタは保存しない。欠番は埋めない。
- **編集可**。現地の樹名札に手書きする運用が出てきた場合に、短い記号
  （`KOJI` など）へ変更できるようにする。
- 制約は次の2つだけ。
  - **半角英数とハイフンのみ**（CSVやファイル名で壊れないため）
  - **重複不可**（保存前にチェックし、重複していたらエラーで止める）
- コード変更時は、**その公園に紐づく既存樹木の `treeNo` と `parkCode` を一括で書き換えるか**を
  確認ダイアログで尋ねる。「書き換える／このままにする」を選べるようにし、
  書き換えは単一の IndexedDB トランザクションで実行する。
- 公園コードが読みやすい必要はない。一覧・地図・CSVにはすべて**公園名**を表示する。

#### 樹木番号

書式: `{公園コード}{連番3桁}`（区切りの「-」は入れない） 例 `P001004`

> 実際の採番は14章のテープ番号ベース（`treeNoFromTape`）で行う。ここは
> テープ番号を使わない場合の後方互換として残してある（`nextTreeNo`）。

- 連番は「その公園に既存する樹木の連番の最大値 ＋1」を**毎回計算する**。
  カウンタを保存しない（削除時のズレを防ぐため）。**欠番は埋めない。**
- 登録画面には次の番号をプレビュー表示する。**編集可能**にする（手で上書きできる）。
- 同一公園内で番号が重複したら保存前に警告する（保存自体は許可し、一覧で重複バッジを出す）。

---

## 5. データ層設計（Dexie）

```js
db.version(1).stores({
  parks:    'id, code, name, lastUsedAt',
  trees:    'id, parkId, treeNo, species, updatedAt, [parkId+treeNo]',
  photos:   'id, treeId',        // 写真は別テーブル（一覧の描画を軽くするため）
  settings: 'key'                // 樹種の使用履歴など単一値
});

// v2: スプレッドシート連携（13章）のために2つ足した
db.version(2).stores({
  // …v1と同じ4つに加えて
  pending:   'id, table',        // まだシートへ送っていないレコードの控え
  deletions: 'id, table, at'     // 削除の記録（tombstone＝墓標）。削除を他端末へ伝える
});

// v3: 国交省様式への再設計に伴うデータ移行（索引は v2 と同じ。中身だけ書き換える）
//   - 葉の茂り 濃/普/ま/ほ → '1'/'2'/'3'/'4'
//   - 旧 trunkDamage は消さず trunkDamageLegacy へ退避（亀裂か傾斜かは推測で振り分けない）
//   - alertLevel を付け直し、変換したレコードを送信待ちへ積み直す
```

### park

```js
{
  id,            // UUID
  code,          // 公園コード 'P001'（自動付与・編集可・重複不可）
  name,          // '皇子が丘公園'
  lat, lng,      // 代表座標（任意）
  note,
  lastUsedAt, createdAt, updatedAt
}
```

### tree

```js
{
  id,                    // UUID（共通スキーマの主キー。10章 案C）
  parkId,
  parkCode,              // 書き出し時に公園コードが単体で追えるよう非正規化して持つ
  tapeNo,                // テープ番号 'A0201'（14章。連番4桁）
  treeNo,                // 'P001A0201'（公園コード＋テープ番号。区切りの「-」は無い）
  species,               // 樹種和名。'不明' も可

  // 点検内容（紙のチェックシート1ページ目。14章）
  height,                // 樹高(m) 数値。目分量
  crownWidth,            // 樹冠幅(m) 任意（PLATEAU veg:crownDiameter）
  vegClass,              // 区分 '高木'|'中木'|'低木' 任意（PLATEAU veg:class）
  leafDensity,           // 葉の茂り '1'〜'4'（1=濃 … 4=ほとんどない。表示は濃/普/ま/ほ）
  fungus,                // キノコ '有'|'無'|'未'
  fungusPart,            // キノコ部位 '根・幹' のように '・' でつないだ文字列
  cavity,                // 空洞・傷 '有'|'無'（穴・樹皮の剥離。割れ目は trunkCrack へ）
  cavityPart,            // 空洞・傷の位置 '根・幹'（根 幹 枝 のみ）
  trunkSway,             // 樹幹の揺らぎ '有'|'無'（国交省 主要項目）
  trunkLean,             // 樹幹の不自然な傾斜 '有'|'無'（同上）
  trunkCrack,            // 樹幹の亀裂 '有'|'無'（同上。旧 trunkDamage をここへ整理）
  barkInclusion,         // 結合部の異常（入り皮）'有'|'無'
  frass,                 // フラス '有'|'無'|'未'
  vigor,                 // 活力度・樹勢 '1'〜'4'（1=良い … 4=枯死。国交省）
  treeForm,              // 活力度・樹形 '1'〜'4'（同上）
  envRoad,               // 周辺環境 道路園路の有無 '有'|'無'
  envWire,               // 周辺環境 電線の有無 '有'|'無'
  envBuilding,           // 周辺環境 建物の有無 '有'|'無'
  envNote,               // 周辺環境 備考（自由記入）
  caution,               // 注意（自由記入）
  photoCount,            // 写真の枚数（紙の「写真」欄。写真本体は photos テーブル）
  alertLevel,            // 重点観察区分 3=重点/2=注意/1=通常/0=未点検（保存のたびに付け直す派生値）
  trunkDamageLegacy,     // 旧「幹の損傷」の退避（要再確認の目印。新規入力はしない）

  // 調査情報（紙の表頭。登録のたびに引き継ぐ）
  surveyDate,            // 調査日 '2026-08-20'
  surveyor,              // 調査者
  tapeRoll,              // テープロール 'A'|'B'|'C'

  lat, lng, accuracy, coordSource,
  girth,                 // 幹周(cm) 任意
  note,                  // メモ 任意（「注意」とは別）
  registeredAt,          // 登録日時
  updatedAt              // 更新のたびに更新（マージ時の競合解決に使う）
}
```

### photo

```js
{ id, treeId, dataUrl, order, takenAt }
```

- 保存前にリサイズする: **長辺 1280px / JPEG 品質 0.7**。
- 1本あたり最大3枚。
- IndexedDB の容量は無限ではない。設定タブに**概算使用量**（`navigator.storage.estimate()`）を表示する。

---

## 6. 画面構成

タブは4つ。**起動時は「登録」タブ**を開く。

### 6-1. 登録タブ（最重要・ここの摩擦をゼロにする）

上から順に:

紙のチェックシートと見比べながら入れられるよう、**並びは紙の順にそろえる**（14章）。

1. **公園セレクタ** — ボタン式（最近使った公園が左）。未登録なら「公園を登録してください」導線
   （紙の「場所」にあたる）
2. **調査情報（折りたたみ）** — 調査日 / 調査者 / テープロール（紙の表頭。14章）
3. **テープ番号・樹木番号** — テープ番号を入れると `{公園コード}{テープ番号}`（区切りの
   「-」は無い）が入る。樹木番号は**編集可能**（手で上書きできる）
4. **樹種タイル** — よく使う樹種を12枚のタイルで表示（使用回数の降順）。
   末尾に「その他（入力）」と「不明」を常設。**前回選択した樹種を初期選択にする**
5. **点検内容** — 樹高 / 葉の茂り / キノコ / キノコ部位 / 空洞・傷 / 空洞・傷の位置 /
   幹の損傷 / 結合部の異常(入り皮) / フラス / 周辺環境（道路園路・電線・建物・備考）/
   注意（14章）
6. **座標ブロック**（3章の仕様そのまま）
7. **写真** — 「📷 撮影」ボタン（`<input type="file" accept="image/*" capture="environment">`）。
   サムネイル横並び、タップで削除（紙の「写真」欄にあたる）
8. **任意項目（折りたたみ）** — 幹周 / メモ（樹高は点検内容へ移した）
9. **固定フッターの大きな［登録］ボタン**

**登録直後の挙動（連続登録のため）**
- トースト表示「P001-012 を登録しました」
- 公園・樹種・**調査情報**は**保持**する
- 座標・写真・任意項目・**点検内容**は**クリア**する
- 番号プレビューを +1 し、**テープ番号もその番号にそろえる**

目標: 公園と樹種が同じなら「📍現在地 → 📷撮影 → 登録」の3タップ。

### 6-2. 一覧タブ

- 上部に公園タブ（横スクロール）
- カード表示: 樹木番号 / 樹種 / 葉の茂り / サムネイル / 座標の有無 / 登録日
- **点検で「有」だったものはバッジで目立たせる**（キノコ / 空洞・傷 / フラス）
- 検索（樹木番号・樹種・メモ・注意の部分一致）
- タップで編集画面（登録タブと同じフォーム）
- **選択して一括削除**モード（parktreenote v3.1 と同じ方式）

### 6-3. 地図タブ

- 国土地理院タイル。選択中の公園の代表座標を初期表示に使う
- 樹木ピン（樹種ごとに色分け、または一律で番号ラベル）
- ピンをタップ → 樹木番号・樹種・サムネイルのポップアップ →「編集」
- 座標なしの樹木は地図に出ない。件数だけ「座標なし 3件」と隅に表示する

### 6-4. 設定タブ

- 公園の管理（4章）
- 樹種マスタの管理（よく使う樹種の追加・削除・並び替え）
- **紙の調査票** — アプリを使わない人に渡すA4横の用紙を印刷する（14-6）
- **スプレッドシート連携（GAS）** — Web AppのURL・合言葉・自動保存の入切・接続テスト・
  今すぐ同期・全件送信（13章）
- **バックアップ** — JSON書き出し／読み込み（7章）
- **書き出し** — CSV / GeoJSON
- ストレージ使用量の概算表示
- 「最終バックアップから◯日」の表示（v27 と同じ考え方）

---

## 7. バックアップ・書き出し

| 形式 | 内容 | 用途 |
|---|---|---|
| **JSON（フルバックアップ）** | parks + trees + photos（Base64込み） | 端末移行・復元 |
| **JSON（軽量）** | parks + trees（写真なし） | 他アプリへの受け渡し |
| **CSV** | trees（写真なし・UTF-8 BOM付き） | Excel・台帳への転記 |
| **GeoJSON** | 座標のある trees | GIS・地図ソフトへの取込 |

**JSON読み込みは「追記マージ」を既定にする**（全上書きではない）。
- `id` 一致 → `updatedAt` が新しい方を採用
- 未知の `id` → 追加
- 実行前に「追加◯件 / 更新◯件」を確認ダイアログで提示する

CSVは UTF-8 BOM 付きで出力する（Excelの文字化け対策）。

---

## 8. フォルダ構成（案）

```
kanntantreeconnect/
├─ index.html
├─ vite.config.js          # base: '/kanntantreeconnect/'
├─ CLAUDE.md
├─ public/
│   └─ manifest.json       # ホーム画面追加用（SWは入れない）
└─ src/
   ├─ main.jsx
   ├─ App.jsx
   ├─ db/db.js                    # Dexie定義
   ├─ lib/
   │   ├─ geo.js                  # Googleマップリンクの座標抽出・現在地取得
   │   ├─ treeNo.js               # 自動採番（純粋関数）
   │   ├─ image.js                # 写真リサイズ
   │   ├─ io.js                   # JSON/CSV/GeoJSON の書き出し・取込
   │   ├─ gasClient.js            # GAS Web App への通信（13章）
   │   ├─ syncMerge.js            # シートと端末の突き合わせ規則（純粋関数・13章）
   │   ├─ sync.js                 # 起動時の取り込み・自動保存の段取り（13章）
   │   ├─ changeBus.js            # db.js → sync.js の伝言板（循環参照を避ける）
   │   ├─ inspection.js           # 点検内容の選択肢と純粋関数（14章）
   │   └─ printSheet.js           # 紙の調査票（A4横）の組み立て（14-6）
   ├─ components/
   │   ├─ RegisterForm/           # 登録タブ
   │   ├─ LocationPicker/         # ★v27から流用
   │   ├─ SpeciesTiles/           # 樹種タイル
   │   ├─ InspectionFields/       # 点検内容（14章）
   │   ├─ SurveyInfo/             # 調査情報＝紙の表頭（14章）
   │   ├─ PhotoInput/
   │   ├─ TreeList/               # 一覧タブ
   │   ├─ MapView/                # 地図タブ
   │   ├─ SyncBadge/              # ヘッダー右上の同期バッジ（13章）
   │   └─ Settings/               # 設定タブ（公園管理・紙の調査票・GAS連携を含む）
   └─ styles/
gas/
├─ Code.gs                  # スプレッドシート中継 Web App（GASに貼り付ける）
└─ README.md                # 設置手順
```

---

## 9. 実装順

1. Vite + React の骨組み（`base` 設定を先に入れる）
2. **`src/db/db.js`（Dexie）と `src/lib/treeNo.js`（採番の純粋関数）** を先に作る
3. 公園管理（設定タブ）— これがないと何も登録できない
4. `LocationPicker` を v27 から移植して単体で動かす
5. 登録タブ（樹種タイル → 座標 → 写真 → 登録）
6. 一覧タブ
7. 地図タブ
8. バックアップ・書き出し
9. GitHub Pages デプロイ
10. **スプレッドシート連携（GAS）** — 13章。`src/lib/syncMerge.js`（純粋関数）と
    そのテストを先に書き、次に `gas/Code.gs`、最後に `src/lib/sync.js` の順で進める
11. **点検内容（現地チェックシート1ページ目）** — 14章。`src/lib/inspection.js`（選択肢と
    純粋関数）から始め、登録フォーム → 一覧のバッジ → CSV/GeoJSON → GASの列、の順で広げる

採番ロジックにはテストを書く（`node --test`）。欠番あり・削除後・重複時の3ケースを最低限確認する。

---

## 10. 台帳連携（方針・初版では未実装）

### 採用方針

**案A（treeNo を横串キーにしたファイル受け渡し）で運用を始める。
ただしデータ構造は案C（共通スキーマ）を満たしておく。**

**本リポジトリでは、これに加えて案Bにあたる GAS スプレッドシート同期を実装した（13章）。**
ただし同期先は v27 のスプレッドシートではなく、本アプリ専用の新しいスプレッドシート。
v27 側の改修は不要で、シートを共有すれば他職員も同じデータを見られる。
v27 と同じブックにまとめるかは、実際に他職員が使い始めてから判断する。

### 案C 共通スキーマ（tree-core）— 本アプリが必ず満たす最小項目

```js
{ id, parkCode, treeNo, species, lat, lng, updatedAt }
```

- `id` は UUID。アプリをまたいでも衝突しない
- マージ規則は `id` 一致で `updatedAt` の新しい方を採用（v27 と同じ規則）
- 将来 GAS でも Supabase でも中継先を差し替えられる

### 未確認事項（実装前に Koh が判断すること）

【推定・要確認】parktreenote（樹木台帳メモ）は CHANGELOG を読む限り
「樹種＋規格＋本数」の**集計単位**のメモであり、個体単位の記録ではないように見える。
だとすれば個体レベルの突合はそもそも噛み合わない。
その場合、実際に価値のある連携は **公園マスタ（公園コード・公園名・代表座標）の共有** の方になる。
`parktreenote/index.html` の公園まわりを読んで、認識が正しいか確認すること。

### 各案の要約

| 案 | 内容 | 長所 | 短所 |
|---|---|---|---|
| A | treeNo を共通キーにし、CSV/JSONで受け渡し | 実装コスト最小・疎結合・壊れにくい | 手作業。リアルタイム性なし |
| B | v27 の GAS スプレッドシートに「樹木マスタ」シートを追加し双方向同期 | 一元管理。他職員と共有できる | v27側の改修とGAS設定が必要。ネット依存 |
| C | 3アプリ共通の最小スキーマを定義して相互取込 | 中継先を後から差し替えられる | 3アプリ全部に取込機能の実装が必要 |

---

## 11. コーディング方針（全プロジェクト共通）

- UIは日本語、コード内コメントも日本語でよい
- 後から自分で読めるコードを優先。過度な抽象化は避ける
- **差分修正の意味は「無関係な箇所を書き換えない」こと。**
  1件の依頼に伴う複数箇所の連動修正はまとめて実施してよい。
  事前合意が必要なのは「設計変更」「ファイル構成変更」「大幅な書き換え」のみ
- **修正報告は簡潔に。** 構文チェック・テストの結果は問題なければ一言で報告する
  （例:「npm test OK」）。問題があった場合のみ原因・修正内容を詳しく説明する
- プログラミング用語は初出時に日本語の補足を入れる
- ターミナル操作は省略せず毎回手順を明示する
- 事実と推定を分ける。推定には【推定】を付ける
- フッターに著作権表記を入れる

---

## 12. 開発の始め方

```bash
# 1. プロジェクト作成（React + JavaScript を選択）
npm create vite@latest kanntantreeconnect
cd kanntantreeconnect

# 2. 依存を入れる
npm install
npm install dexie dexie-react-hooks leaflet react-leaflet

# 3. 開発サーバ起動
npm run dev
```

その後この CLAUDE.md をリポジトリ直下に置き、`src/db/db.js` と `src/lib/treeNo.js` から着手する。

---

## 13. スプレッドシート連携（GAS）

kantantree との差分はここだけ。設置手順は [`gas/README.md`](./gas/README.md)。

### 13-1. なぜ GAS を挟むのか

ブラウザから Google のスプレッドシートを直接読み書きするには OAuth（Googleログイン）と
APIキーが要る。フロントにキーを置くと公開リポジトリから漏れるため、
**GAS の Web App を中継役にして、シートを触るのは GAS 側だけ**にしている。
アプリ側が持つのは Web App のURL（と任意の合言葉）だけで、これは端末内（IndexedDB）に保存する。
**コードに直書きしない。チャット・コミット・スクショにも載せない**（腐朽菌PoCと同じ扱い）。

```
アプリ（GitHub Pages）  ──POST（JSON）──▶  GAS Web App  ──▶  スプレッドシート
                        ◀──JSON────────
```

送信の Content-Type は **`text/plain`**。`application/json` にするとブラウザが
プリフライト（事前確認のOPTIONSリクエスト）を投げてしまい、GAS Web App はそれに
CORSヘッダーを返せないので必ず失敗する。中身はJSON文字列のままでよい。
（腐朽菌PoCのGemini中継でも同じ回避策を採っている）

### 13-2. 3つの動き

| いつ | 何をするか | 実装 |
|---|---|---|
| **アプリ起動時** | シートの内容を端末に取り込む → 送信待ちがあれば送る | `syncOnStartup()`（`App.jsx` の `useEffect`） |
| **書き換えのたび** | 2.5秒待ってまとめてシートへ送る（自動保存） | `startAutoSync()` → `changeBus` で db.js の変更を受けて debounce |
| **手動** | ヘッダー右上のバッジ／設定タブの「今すぐ同期」 | `syncNow()`（取り込み → 送信の順） |

先に取り込んでから送るのは、他の端末が直していた場合に
「新しい方を採用」の判定をしてから送るため。

### 13-3. 送るもの・送らないもの

| | シートへ送る | 理由 |
|---|---|---|
| parks（公園） | ○ | |
| trees（樹木） | ○（`thumb` は外す） | 点検内容（14章）も含めて送る。サムネイルはBase64で数十KB、セル上限（5万文字）を圧迫するので外す |
| photos（写真） | **×** | 同上。端末内＋JSONバックアップで扱う |
| settings（樹種マスタ・使用回数・前回の調査情報） | **×** | 端末ごとの使い勝手の設定なので共有しない |

`park.lastUsedAt`（公園タブの並び順）は `updatedAt` を動かさないので実質同期されない。
これは端末ごとの使い勝手の値なので、そのままでよい。

### 13-4. 突き合わせの規則（`src/lib/syncMerge.js`）

7章のJSON取込マージと**同じ規則**にそろえてある。GAS側（`gas/Code.gs`）も同じ規則で動く。

- `id` が一致したら `updatedAt` の新しい方を採用
- 知らない `id` は追加
- 削除は **`deletions` シート（tombstone＝墓標）** で伝える。
  削除より後に更新されたレコードは消さない（更新が勝つ＝復活する）
- シート側が勝つときも、端末の `thumb` は消さずに引き継ぐ

純粋関数（DOMにもDBにも触らない）にしてあるので `npm test` で確かめられる
（`test/syncMerge.test.js`）。**規則を変えるときは、必ずGAS側の `upsert_` /
`applyDeletions_` も一緒に直すこと。**

### 13-5. 取りこぼさないための仕組み

書き換えのたびに `pending` テーブル（送信待ちの控え）へ id を積む。**保存と同じ
トランザクション**で積むので、「保存できたのに送信待ちに載っていない」は起きない。
送信できたぶんだけ `pending` から消す。したがって:

- 圏外で登録しても消えない。電波が戻ったとき（`online` イベント）・アプリを開き直したときに送る
- 送信に失敗したら 5秒 → 15秒 → 60秒 → 3分 と間隔を空けて再送する
- ヘッダー右上のバッジに「◯件待ち」「未送信あり」が出る（タップで今すぐ同期）

シートから取り込んだレコードは `pending` に積まない（送り返す無限往復を避けるため）。

### 13-6. 変えるときの注意

- **列を足すときは3か所そろえる**: `gas/Code.gs` の `SHEETS`、アプリ側の保存処理、
  `gas/README.md` の表。列の順番がそのままシートの列になる。
- 日時は ISO 8601 の文字列。シートが勝手に日付型へ変換しないよう、
  数値列以外は「書式なしテキスト」にしている（`getSheet_`）。
- GASを直したら「デプロイを管理 → 編集 → 新しいバージョン」で再デプロイする。
  こうするとURLが変わらないので、アプリ側の設定を直さずに済む。
- 実機確認は `npm run dev`（ローカル）か公開URLで。GASのURLを入れないと同期は動かない
  （入れなければ従来どおり端末内だけで完結する）。

---

## 14. 点検内容（現地チェックシート1ページ目）

紙の **「樹木点検 現地チェックシート」** をそのままアプリに写したもの。
選択肢の定義は `src/lib/inspection.js` に置き、画面・CSV・GASのどこからも同じものを使う。

### 14-1. 紙との対応

| 紙の欄 | フィールド | 選択肢 |
|---|---|---|
| 場所 | `parkId` / `parkCode` | 公園セレクタで選ぶ |
| 調査日 | `surveyDate` | `2026-08-20`（日付入力） |
| 調査者 | `surveyor` | 自由記入 |
| テープロール | `tapeRoll` | `A` `B` `C` |
| テープ番号 | `tapeNo` | `A0201`（ロール記号＋連番4桁）。記号なしの `0201`、手書きの `12b` も可 |
| 樹木番号（公園コード+テープ番号） | `treeNo` | `P001A0201`。区切りの「-」は入れず、テープ番号から自動で作る |
| 樹種（不明可） | `species` | 樹種タイル。`不明` も可 |
| 樹高 | `height` | m。よく使う高さ（3/5/8/10/15/20）をタップ＋自由入力。目分量でよい |
| （紙になし）樹冠幅 | `crownWidth` | m。枝の広がりの差し渡し。任意。PLATEAU `veg:crownDiameter` |
| （紙になし）区分 | `vegClass` | `高木` / `中木` / `低木`。任意。PLATEAU `veg:class` |
| 葉の茂り | `leafDensity` | `濃`＝枝先まで密 / `普`＝ふつう / `ま`＝まばら（樹冠から空が透ける）/ `ほ`＝ほとんどない |
| キノコ | `fungus` | `有` / `無`＝見たが無かった / `未`＝見ていない・見えない |
| キノコ部位 | `fungusPart` | `根`＝根元（地際〜50cm・露出根）/ `幹`＝根元より上の生きた幹 / `枝`＝枝の付け根 / `枯`＝枯枝・枯幹 / `不`＝不明（**複数可**） |
| 空洞・傷 | `cavity` | `有` / `無`。穴・樹皮の広範囲な剥離・割れ目。大きさの基準は設けない |
| 空洞・傷の位置 | `cavityPart` | `根` `幹` `枝`（**複数可**）。`有` のときだけ出す |
| 幹の損傷 | `trunkDamage` | `有` / `無` |
| 結合部の異常(入り皮) | `barkInclusion` | `有` / `無`。二又や枝の付け根に樹皮が巻き込まれた状態 |
| フラス | `frass` | `有` / `無` / `未`。木くずとフンが混ざったうどん状・かりんとう状の排出物 |
| 周辺環境 道路園路の有無 | `envRoad` | `有` / `無` |
| 周辺環境 電線の有無 | `envWire` | `有` / `無` |
| 周辺環境 建物の有無 | `envBuilding` | `有` / `無` |
| 周辺環境 備考欄 | `envNote` | 自由記入 |
| 注意 | `caution` | 気になった木に印（自由記入） |
| 写真 | `photoCount`（＋ `photos` テーブル） | 撮った枚数。写真本体は端末内 |

**1行＝1本**（紙の注記と同じ）。株立ちも1本として1件で登録する。

選択肢の説明文は紙の「記入ルールと運用」3. 記入の凡例 をそのまま使っている
（`inspection.js` の `hint`）。**紙を直したらここも直すこと。**

### 14-2. テープ番号の採番（樹木番号とは別の規則）

紙の運用ルール2:「ロールは使い切るまで**場所をまたいで連続して**使う。場所ごとに
区切らない」。したがって:

| | 採番の範囲 | 実装 |
|---|---|---|
| 公園コード | 端末全体 | `nextParkCode` |
| **テープ番号** | **端末全体（公園で区切らない）・ロール記号ごと** | `nextTapeNo` / `suggestTapeNo` |
| 樹木番号 | テープ番号から作る（`{公園コード}{テープ番号}`。区切りの「-」は無い） | `treeNoFromTape` |

- 連番は**4桁にそろえる**（`0001` から。ラベルに巻いたときに桁がそろって読みやすいため）。
  ロール記号（A・B・C）を選んでいれば、その記号のものだけを見て `A0201` → `A0202`。
  記号なしで書いている場合は数字の続き。
- 紙のルールでは「一度使った番号は二度と使わない（伐採後も使い回さない）」。
  アプリは登録済みの最大値＋1を出すので、**いちばん大きい番号の記録を消すと
  その番号を再び提案する**。カウンタは保存しない方針（4-2）を優先しているため。
- `nextTreeNo`（公園ごとの連番）は、テープ番号を使わない場合の後方互換として残してある。

### 14-3. 見つけたらすぐ連絡（紙の運用ルール4）

`urgentNotes()` が、入力した直後に画面へ注意書きを出す。

- フラス「有」→ 全景と近景を撮影し、可能なら持ち帰る
- キノコ「有」＋部位「根」→ 根元のキノコは大小に関わらずすぐ連絡
  （キノコの大小・有無は腐朽の程度の目安にならない。小さくても・無くても内部の腐朽が進んでいることがある）
- 空洞・傷「有」＋道路・園路「有」→ 倒れたときの影響が大きいので連絡

傾き・頭上の枯れ大枝は入力項目にないので、「注意」欄に書いてもらう。

### 14-4. 決めごと

- **テープ番号 → 樹木番号**。テープ番号を入れると `{公園コード}{テープ番号}`（区切りの
  「-」は入れない）が入る（`treeNoFromTape`）。数字だけなら3桁にそろえ、`A0201` のような
  記号付きはそのまま付く。テープ番号・樹木番号のどちらも手で直せる。直したあとは自動で上書きしない。
- **キノコ部位・空洞・傷の位置は複数選べる**が、CSV・シート・JSONで形を揃えたいので
  配列ではなく `'根・幹'` のような**文字列1つ**で持つ。並びは常に紙の順
  （キノコ部位: 根 幹 枝 枯 不 ／ 空洞・傷の位置: 根 幹 枝）。
  `partList` / `togglePart` は選択肢の一覧を引数で受けるので、両方で使い回せる。
- **樹高は選択肢＋自由入力**。現場では測らない（巻尺を使わない）ので、
  よく使う高さ（`HEIGHT_PRESETS`）を押すだけで入るようにし、他の値だけ手で打つ。
  値は数値（m）のまま持つ。
- **周辺環境は「すべて無」ボタン**を置いた。何も無い場所が続くときの手数を減らすため。
- **もう一度押すと選択が外れる**（間違えて押したときに戻せるように）。
- **キノコが「無」「未」になったら部位は消す**（紙でも部位は書かないため）。
  空洞・傷が「無」になったときの位置も同じ。
- **調査情報は登録のたびに引き継ぐ**（1枚のシートで共通の項目のため）。
  `settings` の `lastSurvey` に覚えさせる。端末ごとの値なのでシートには送らない。
- **点検内容は必須にしない。** 座標と同じく、後から入れられる。
- **健全度・活力度の判定はしない。**「葉の茂り」は簡易な観察項目であり、国や
  日本緑化センター等が定める活力度・健全度の区分とは別のもの（紙の「やらないこと」）。
- 紙の運用では巻尺・測定機器を使わない（幹周は測らず、スタッフを立てて撮影する）。
  `height` / `girth` は任意項目のまま折りたたみに置いてある。

### 14-5. どこに出るか

- 一覧タブ: キノコ・空洞・傷・幹の損傷・入り皮・フラスが `有` のものは**赤いバッジ**を出す。
  葉の茂りは樹種の隣に小さく出す。検索は「注意」も対象にする。
- CSV / GeoJSON: 上の表の順で全項目を書き出す（`src/lib/io.js`）。
- スプレッドシート: `trees` シートの列も同じ並び（13章・`gas/Code.gs` の `SHEETS`）。

**項目を足すときは4か所そろえる**: `inspection.js` / 登録フォームの保存処理 /
`io.js` の `CSV_COLUMNS`・GeoJSON / `gas/Code.gs` の `SHEETS.trees`（＋`gas/README.md` の表）。
`test/inspection.test.js` に、GASの列から漏れていないかを機械で確かめるテストを入れてある。

### 14-6. 紙の調査票（アプリを使わない人向け）

アプリを入れていない人にも同じ内容を書いてもらうための**印刷用の用紙**。
設定タブの「紙の調査票」から開き、ブラウザの印刷でA4横に出す。

- **A4横・2枚。** 1枚目が記入表（1行＝1本）、2枚目が記入の凡例と運用のきまり。
- **紙は `inspection.js` の定義から組み立てる**（`src/lib/printSheet.js`）。
  項目・並び・選択肢・凡例の説明文をアプリと共有しているので、
  **アプリに項目を足せば紙にも自動でつく**。手で紙を直す作業は発生しない。
  したがって14-5の「4か所」に紙は含まれない（自動でついてくる）。
- **`test/printSheet.test.js` がずれを止める。** `INSPECTION_FIELDS` の全項目が
  用紙にあるか・並びが同じか・選択肢が選択肢配列から来ているか・
  凡例に全 `hint` があるかを機械で確かめる。
- 列幅は `SHEET_COLUMNS` の `mm` の合計＝**281mm**（A4横297mm − 余白8mm×2）。
  **列を足したら、どこかを削って合計281mmに戻すこと。** 超えると印刷が縮んで読めなくなる。
- **見出しは横書きで折り返す**（`word-break: break-all`・高さ13mm）。
  縦書き（`writing-mode`）は環境のフォントに強く依存し、印刷先で崩れても
  こちらで気づけないので使わない。
- 行の高さは行数から決める（`rowHeightMm`）。10 / 14 / 18行のどれでも1枚に収まり、
  行数が少ないときは行が広くなる。手書きなら14行が目安。
- 場所・調査日・調査者・テープロールは、印字するか空欄（手書き）かを選べる。
- 書き方は**〇で囲む**。空欄のままでもよい（分からない・見ていない項目は無理に埋めない）
  ＝紙の運用と同じ。
- **用紙はアプリの中に重ねて出す**（`SheetViewer.jsx`）。**別タブ（`window.open`）にはしない。**
  ホーム画面から起動したiPhoneには戻るボタンが無く、別タブに移るとアプリへ帰れなくなるため。
  `createPortal` で `body` 直下（`#root` の兄弟）に出し、印刷のときは app.css の
  `@media print` で `#root` だけを隠して用紙を原寸で出す。
- 用紙のCSSは `.sheetdoc` の中だけに効くようにしてある（`css(scope)`）。
  `h1` や `table` のような広い指定がアプリ側へ漏れないため。
  `buildSurveySheetHtml()` が単体のHTML文書、`buildSurveySheetFragment()` がアプリ埋め込み用で、
  中身は同じものを使う。`test/printSheet.test.js` がスコープ漏れを機械で止める。
- 画面では横281mmが入らないので、開いた直後は画面幅に収まるまで縮めて全体を見せ、
  「🔍 拡大」で原寸に戻せる。縮小は `@media screen` の中だけに置くので**印刷は常に原寸**。
  倍率は縮める前の実寸を一度だけ測って決める（縮んだ寸法で測り直すと合わなくなる）。
- パソコンへ持って行きたいときのために「HTMLで保存」も置いてある（単体で開ける文書）。

---

© 2026 Koh Kitsukawa. All rights reserved.
